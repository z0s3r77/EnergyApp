{/*                {allPrices !== null ? (
                    Object.values(allPrices).map((priceData, index) => (
                        <View key={index}>
                            <Text>Date: {priceData.date}</Text>
                            <Text>Hour: {priceData.hour}</Text>
                            <Text>Is Cheap: {priceData['is-cheap'].toString()}</Text>
                            <Text>Is Under Avg: {priceData['is-under-avg'].toString()}</Text>
                            <Text>Market: {priceData.market}</Text>
                            <Text>Price: {priceData.price}</Text>
                            <Text>Units: {priceData.units}</Text>
                        </View>
                    ))
                ) : (
                    <Text>allPrices is null</Text>
                )}*/}